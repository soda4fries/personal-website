:8080 {
    root * ./dist
    encode zstd gzip
    file_server

    @static {
        path *.js *.css *.ico *.png *.jpg *.jpeg *.svg *.woff2
    }
    header @static Cache-Control "public, max-age=31536000, immutable"

    header {
        X-Content-Type-Options "nosniff"
        Referrer-Policy "strict-origin-when-cross-origin"
    }

    log {
        output stdout
    }
}
